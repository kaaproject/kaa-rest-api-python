[project]
name = "kaa-rest-client"
version = "0.1.2"
description = "Python client library for KaaIoT platform REST APIs"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "httpx>=0.28",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
